---
const { flip = false } = Astro.props
---
<div class={ "container mx-auto max-w-6xl h-96 grid grid-cols-2"}
     {...Astro.props}>
    {
      /* Default to the left side */
      !flip && <slot name="image"/>
    }
    <slot/>
    {
      flip && <slot name="image"/>
    }
</div>
