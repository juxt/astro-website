---
import { getImage, Image } from 'astro:assets'
import Section from '@components/team/Section.astro'

const {author} = Astro.props;

const quoteBg = await getImage({src: import("../../assets/team/quote-bg.png")})
---
<Section bgStyle={`background-image: url('${quoteBg.src}');`}
  bgClass="bg-no-repeat bg-center bg-cover"
  className="py-40 max-w-4xl"
  {...Astro.props}>
  <blockquote>
    <p class="text-2xl font-medium text-center">
      "<slot/>"
    </p>
  </blockquote>
  {
    author &&
    <p class="pt-10 text-md font-medium text-center">
      {author}
    </p>
  }
</Section>
