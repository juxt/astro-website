---
import AssetImage from '@components/team/AssetImage.astro'

const { imgClass, className } = Astro.props;
---
<div class={"flex flex-1 justify-center items-center " + className}>
    <AssetImage class={"flex-1 h-full object-cover object-center " + imgClass} {...Astro.props} />
</div>
